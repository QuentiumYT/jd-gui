#!/usr/bin/make -f
# -*- makefile -*-

# Uncomment this to turn on verbose mode.
#export DH_VERBOSE=1

%:
	dh $@ --parallel

override_dh_auto_build:
	chmod +x gradlew*
	$(CURDIR)/gradlew build --no-daemon

override_dh_auto_clean:
	chmod +x gradlew*
	$(CURDIR)/gradlew clean --no-daemon
